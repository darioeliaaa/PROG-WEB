// Ambiente di produzione (usato da `ng build --configuration production`,
// che è quello che Vercel esegue di default per una build Angular).
//
// ⚠️ DA COMPILARE: sostituisci il valore qui sotto con l'URL reale del
// backend una volta che l'hai messo online (es. su Render). Poi fai commit
// e push — Vercel rifà automaticamente il deploy.
export const environment = {
  production: true,
  apiUrl: 'https://SOSTITUISCI-CON-IL-TUO-BACKEND.onrender.com',
};
